server:
  port: 9090

spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: "jdbc:postgresql://localhost:5432/MovieApp2"
    username: "postgres"
    password: "*****"
  jpa:
    hibernate:
      ddl-auto: update
